#include <stdio.h>
#include <stdlib.h>
// data structure = a way of organizing data.

// Queue is a linear data structure.
// FIFO = First in, First Out but can also be LILO = last in, last out.

// Você ta com problema pra entender, fifo , lifo , filo.
// Na queue você vai usar "rear" pra falar tail e "front" pra falar da head.
// Insertion done on the rear.
// Deletion made on the front.

// Insertion = enqueue
// Deletion = dequeue

//Criar dois ponteiros extras : front and rear.
int main (){
    int front = -1, rear = -1; //empty queue
    // enqueue()
    // dequeue()
    // peek()/ front()
    // isFull() //will return true if the queue is full.
    // isEmpty() 
    return 0;
}